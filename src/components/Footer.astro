---
import { footerMenu, legalMenu } from '@data/menu';
import Logo from '@components/Logo.astro';
import Button from '@components/ui/Button.astro';
import { siteConfig } from '@data/config';
//import mainLogo from '../assets/images/home/Tris_Logo.png';

interface ButtonProps {
    text: string;
    link: string;
    target?: string;
}

interface Props {
    footerCta?: {
        title: string;
        description: string;
        hideCta: boolean;
        button: ButtonProps;
    };
}

//const { defaultCta } = Astro.props;

/* 
const defaultCta = {
    title: 'Start Your Learning Journey Now',
    description: 'Contact us to discover how our training can elevate your career and organisation.',
    hideCta: true,
    button: {
        text: 'Contact Us',
        link: '/contact',
        target: '_blank',
    },
}; */

// const cta = defaultCta;
const currentYear = new Date().getFullYear();
---

<footer class={`text-white pb-small `}>
    <div class="site-container mx-auto px-4">
        <!-- {
            !cta.hideCta && (
                <div class="text-center border-b border-background-light mb-12 py-small">
                    <h2 class="text-3xl font-bold mb-4" data-aos="fade-up">
                        {cta.title}
                    </h2>
                    <p class="text-white mb-6 max-w-2xl mx-auto" data-aos="fade-up" data-aos-delay="100">
                        {cta.description}
                    </p>
                    <Button
                        href={cta.button.link}
                        variant="ghostLight"
                        size="lg"
                        data-aos="fade-up"
                        data-aos-delay="200"
                        {...(cta.button.target && { target: cta.button.target })}
                    >
                        {cta.button.text}
                    </Button>
                </div>
            )
        } -->

        <div class="grid grid-cols-4 md:grid-cols-4 gap-8 pb-8 border-b border-background-light">
            {/* colum 1: brand and descriptions */}
            <div class="column col-span-4">
                <Logo variant="footer" />
                <p class="mt-4 text-white">Empowering your digital journey with innovative solutions.</p>
            </div>

            {/* Column 2: Quick Links */}
            <div class="column col-span-2">
                <h3 class="text-lg font-semibold mb-4">Quick Links</h3>
                <nav>
                    <ul class="space-x-1">
                        {
                            footerMenu.map((item) => (
                                <li>
                                    <a href={item.link} class="text-body-dark hover:text-white transition-colors">
                                        {item.name}
                                    </a>
                                </li>
                            ))
                        }
                    </ul>
                </nav>
            </div>

            <div class="column col-span-2">
                <h3 class="text-lg font-semibold mb-4">Contact Us</h3>
                <nav>
                    <ul class="space-x-2">
                        {
                            footerMenu.map((item) => (
                                <li>
                                    <a href={item.link} class="text-body-dark hover:text-white transition-colors">
                                        {item.name}
                                    </a>
                                </li>
                            ))
                        }
                    </ul>
                </nav>
            </div>

            {/* Column 3 & 4 are currently empty. You can add more divs with class="col-span-1" here */}
        </div>

        <div class="mt-8 flex flex-col md:flex-row justify-between items-center">
            <div class="text-body-dark text-sm">
                © 2024 {siteConfig.companyName}. All rights reserved.
            </div>

            <nav class="mt-4 md:mt-0">
                <ul class="flex flex-wrap gap-6">
                    {
                        legalMenu.map((item) => (
                            <li>
                                <a href={item.link} class="text-sm text-body-dark hover:text-white transition-colors">
                                    {item.name}
                                </a>
                            </li>
                        ))
                    }
                </ul>
            </nav>
        </div>
    </div>
</footer>
